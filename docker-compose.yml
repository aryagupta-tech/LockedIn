services:
  lockedin:
    build: .
    container_name: lockedin
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - waitlist-data:/app/data
    environment:
      - ADMIN_KEY=${ADMIN_KEY:-change-me}

volumes:
  waitlist-data:
