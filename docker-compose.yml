services:
  lockedin:
    build: .
    container_name: lockedin
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      - ADMIN_KEY=${ADMIN_KEY:-change-me}
